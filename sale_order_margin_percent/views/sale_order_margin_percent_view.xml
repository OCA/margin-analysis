<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_sale_order_margin_percent_form" model="ir.ui.view">
        <field name="name">view.sale.order.margin.percent.form</field>
        <field name="model">sale.order</field>
        <field name='inherit_id' ref='sale_margin.sale_margin_sale_order' />
        <field name="arch" type="xml">
            <field name="margin" position="after">
                <field name="percent" widget="progressbar" />
            </field>
            <xpath expr="//field[@name='order_line']/form//field[@name='tax_id']" position="before">
                <field name="percent" string="Margin" widget="progressbar"/>
            </xpath>
           <xpath expr="//field[@name='order_line']/tree//field[@name='price_subtotal']" position="after">
                <field name="percent" string="Margin"/>
                <!-- The button is used only to display % icon
                    since is not possible to add html to tree view yet-->
                <button icon="fa-percent"/>
            </xpath>

        </field>
    </record>
</odoo>
